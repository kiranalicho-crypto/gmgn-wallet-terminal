name: Legacy Dune Foundation (Do Not Use)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Bilgi
        run: |
          echo "Bu Dune workflow'u ücretsiz plandaki performance-tier engeli nedeniyle kullanımdan kaldırıldı."
          echo "Actions > Wallet Intelligence Pipeline workflow'unu çalıştırın."
